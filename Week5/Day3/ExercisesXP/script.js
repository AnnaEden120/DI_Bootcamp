//Exercise1
//1+2.
let compare=function (compareToTen(num);

	let options = new Promise(compareToTen(resolve, reject) {
		if (num < 10) {
			resolve("Ok");
		} else if (num>10) {
			resolve("Not ok");
		} else {
			reject("Not ok at all");
		}
	});


	options.then(compareToTen(num){
		console.log("This is", JSON.string(value));
	}

	options.catch(compareToTen(num){
		consoloe.log("Oh no", value);
	}

	options.finally(compareToTen(num){
		consoloe.log("Sorry,", value);
	}