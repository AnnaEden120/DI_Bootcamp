//first
//1.
let arr=(["word1", "word2", "word3"]);

function makeAllCaps(arr) {
	const promise= new Promise((resolve, reject){
		if (arr==string) {
			resolve(arr.toUpperCase());     
		} else {
			promise.reject("Not good")
		}
	});
}

//2.
const arr2=arr.toUpperCase();

function sortWords(arr2){
	const promise2= new Promise((resolve, reject){
		if (arr2.length>4){
			resolve(arr2.sort());
		} else{
			promise2.reject("No")
		}
	});
}


//Second
//not sure how to solve it...
